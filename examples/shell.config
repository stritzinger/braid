#{
    'shell@Lucas-MacBook-Pro.local' => % change with proper hostname
        #{
            bob => #{
                image => <<"local/braidnode">>,
                connections => [alice]
            },
            alice => #{
                image => <<"local/braidnode">>,
                connections => [bob]
            }
        }
}.
